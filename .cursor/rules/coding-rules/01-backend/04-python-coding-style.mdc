---
description: バックエンド（FastAPI）固有のコーディングスタイルと命名規則
globs: ["**/*.py"]
alwaysApply: false
---

# バックエンド (FastAPI): コーディングスタイル

Python バックエンド固有のコーディングスタイルルールです。
`../00-common/01-coding-style.mdc` よりもさらに詳細なコーディングルールについて記載しています。

## Python固有の命名規則

### 基本的な命名パターン
- **変数・関数名**: `snake_case` を使用
- **クラス名**: `PascalCase` を使用  
- **定数**: `ALL_CAPS_WITH_UNDERSCORES` を使用
- **プライベート属性**: 先頭に `_` を付与（例: `_private_method`）
- **特別なプライベート属性**: 先頭に `__` を付与（例: `__very_private`）

### 具体例
```python
# ✅ 良い例
class UserService:
    MAX_RETRY_COUNT = 3
    
    def __init__(self):
        self._db_connection = None
        self.__internal_state = {}
    
    def calculate_total_amount(self, items: List[Item]) -> int:
        return sum(item.price for item in items)
    
    def _validate_user_data(self, data: dict) -> bool:
        return all(key in data for key in ['name', 'email'])

# ❌ 悪い例
class userService:  # クラス名がsnake_case
    maxRetryCount = 3  # 定数がcamelCase
    
    def calculateTotalAmount(self, items):  # 関数名がcamelCase
        return sum(item.price for item in items)
```

## 型ヒント・アノテーション

### 基本的な使用方法
- すべての関数に**引数と戻り値の型**を明示する
- `typing` モジュールを活用して複雑な型を表現する
- `Optional` や `Union` を適切に使い分ける

### 具体例
```python
from typing import Optional, List, Dict, Union
from datetime import datetime

def process_users(
    users: List[User], 
    created_at: Optional[datetime] = None,
    filters: Dict[str, Union[str, int]] = None
) -> Dict[str, int]:
    """ユーザーデータを処理する。
    
    Args:
        users: 処理対象のユーザーリスト
        created_at: 作成日時フィルター（任意）
        filters: 追加フィルター条件（任意）
    
    Returns:
        処理結果のサマリー辞書
    """
    if filters is None:
        filters = {}
    
    # 処理ロジック
    return {"processed": len(users), "filtered": 0}

# クラスメソッドの型ヒント
class UserRepository:
    def find_by_id(self, user_id: int) -> Optional[User]:
        # 実装
        pass
    
    def save(self, user: User) -> User:
        # 実装
        pass
```

## インポート文の整理

### 基本方針
**Ruffのisort機能を使用してインポート文を自動整理する**

手動でインポート順序を管理する必要はありません。以下のコマンドで自動的にPEP 8準拠の順序に整理されます。

### 自動整理コマンド
```bash
# インポート文のみを整理
uv run ruff check . --select I --fix

# 全体のフォーマット（インポート含む）
uv run ruff format .

# 全体のリンターチェック（インポート含む）
uv run ruff check . --fix
```

### 自動整理される順序
Ruffが以下の順序で自動整理します：
1. **標準ライブラリ** (datetime, typing, os など)
2. **サードパーティライブラリ** (fastapi, sqlalchemy など)
3. **ファーストパーティ** (src.* など、自分のプロジェクト)

各グループ間は自動的に空行で区切られます。

### 設定
`pyproject.toml`で設定済み：
```toml
[tool.ruff.lint.isort]
known-first-party = ["src"]
```

### 避けるべきパターン
```python
# ❌ 避ける: ワイルドカードインポート
from typing import *

# ❌ 避ける: 不要なインポート
import os  # 使用していない場合
```

**注意**: インポート順序は手動で管理せず、必ずformatterに任せてください。
